VITE_API_BASE=http://localhost:8080

# Chrome extension id for the JWT bridge (optional). Load the /extension folder via
# chrome://extensions "Load unpacked", copy the generated Extension ID here, and restart
# the dev server. Leave blank to disable the bridge (the web app works fine without it).
VITE_EXTENSION_ID=
