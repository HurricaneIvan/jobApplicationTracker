SERVER_PORT=8084
SPRING_DATA_MONGODB_URI=mongodb://localhost:27017/tracker

# Retention window: complete tiles older than this many days get archived.
ARCHIVE_AFTER_DAYS=7

# Cron for the archival sweep (Spring 6-field cron). Default: top of every hour.
ARCHIVAL_CRON=0 0 * * * *
